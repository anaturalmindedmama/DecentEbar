name: Build Android APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Force Android 12 Compatibility
        run: |
          find . -name "build.gradle.kts" -o -name "build.gradle" | while read -r file; do
            sed -i 's/minSdk = [0-9]*/minSdk = 31/g' "$file" 2>/dev/null || true
            sed -i 's/minSdkVersion [0-9]*/minSdkVersion 31/g' "$file" 2>/dev/null || true
          done

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew assembleDebug

      - name: Upload APK Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: app-apk
          path: app/build/outputs/apk/debug/app-debug.apk
